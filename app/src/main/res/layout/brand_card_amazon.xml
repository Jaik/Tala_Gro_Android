<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    app:cardCornerRadius="8dp"
    app:cardElevation="6dp"
    app:cardUseCompatPadding="true">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <ImageView
            android:id="@+id/couponImageView"
            android:layout_width="85dp"
            android:layout_height="85dp"
            android:src="@drawable/amazon"
            android:padding="5dp"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:scaleType="centerCrop" />

        <TextView
            android:id="@+id/amazon_coupon_amount_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="1000 Tala Coins"
            android:fontFamily="@font/lexend_semibold"
            android:textSize="17sp"
            android:textColor="@color/black"
            android:layout_alignTop="@+id/couponImageView"
            android:layout_toEndOf="@id/couponImageView"
            android:layout_marginStart="16dp" />

        <TextView
            android:id="@+id/amazon_coupon_description_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/amazon_coupon_amount_tv"
            android:text="Get 10% Discount on Amazon on purchase of minimum $1000 up to $100"
            android:textSize="16sp"
            android:layout_alignBottom="@+id/couponImageView"
            android:textColor="@color/black"
            android:layout_toEndOf="@id/couponImageView"
            android:layout_marginStart="16dp" />
    </RelativeLayout>
</androidx.cardview.widget.CardView>
